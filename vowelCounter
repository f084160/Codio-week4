def vowelCounter(text):
  """Count the number of vowels in a text entered as string"""
  vowel = 0
  for letter in text:
      if letter in "aeiou":
          vowel = vowel + 1
      elif letter[0].lower() in 'aeiou':
          vowel = vowel + 1
  return vowel

#print(vowelCounter("Adios"))
